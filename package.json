{
  "name": "cs-go-score-bot",
  "version": "0.0.1",
  "description": "",
  "main": "app.js",
  "private": true,
  "scripts": {
    "build": "npm install && bower install",
    "dev": "babel source/ -d build/ -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-preset-es2015": "^6.16.0",
    "babel-preset-stage-0": "^6.16.0",
    "nodemon": "^1.11.0"
  },
  "dependencies": {
    "cheerio": "^0.22.0",
    "hltv-match": "^1.0.0",
    "html-to-json": "^0.6.0",
    "moment": "^2.15.1"
  }
}
